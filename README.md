# Analytics Dashboard

A self-contained analytics dashboard with a stats grid, recent activity feed, and sidebar navigation. Built with vanilla HTML, CSS, and JavaScript — no dependencies. Open `index.html` in a browser to view.